name: Release

on:
  push:
    branches:
      - main

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # npm Trusted Publishing (OIDC) requires npm CLI >=11.5.1; Node's bundled npm is older.
      - name: Update npm for Trusted Publishing support
        run: npm install -g npm@^11.5.1

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      - name: Unit tests
        run: pnpm run test:unit

      - name: Create Release Pull Request or Publish to npm
        id: changesets
        uses: changesets/action@v1
        with:
          publish: pnpm release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # TEMPORARY: bootstraps the first publish of @elleffe-tech/whatsapp so a
          # Trusted Publisher can be configured on npmjs.com. Remove this once that
          # package exists and Trusted Publishing (OIDC) is linked to this workflow.
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
